"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.encodedContextCache = void 0;
exports.getEncodedContext = getEncodedContext;
exports.encodeToUtf8Base64 = encodeToUtf8Base64;
exports.encodedContextCache = new Map();
function getEncodedContext(key) {
    if (!key || typeof key !== 'string') {
        return undefined;
    }
    if (exports.encodedContextCache.has(key)) {
        return exports.encodedContextCache.get(key);
    }
    const encoded = encodeToUtf8Base64(key);
    exports.encodedContextCache.set(key, encoded);
    return encoded;
}
/**
 *
 * Helper function to encode a string to base64 in UTF-8
 * Comply with the `_dd.p.usr` standard and avoid non-ASCII characters
 * https://developer.mozilla.org/en-US/docs/Web/API/Window/btoa
 */
function encodeToUtf8Base64(plainText) {
    const bytes = new TextEncoder().encode(plainText);
    const binString = String.fromCodePoint(...bytes);
    return btoa(binString);
}
//# sourceMappingURL=encodedContext.js.map