import { type EndpointBuilder } from '@datadog/browser-core';
import type { RumProfilerTrace } from '../types';
type SendProfileFunction = (trace: RumProfilerTrace, endpointBuilder: EndpointBuilder, applicationId: string, sessionId: string | undefined) => Promise<unknown>;
export declare const transport: {
    sendProfile: SendProfileFunction;
};
export {};
